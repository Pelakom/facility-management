{
  "name": "pelakom-fm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p public/css && cp -f node_modules/material-symbols/material-symbols-*.woff2 ./public/css/ && tailwindcss -i ./src/input.css -o ./public/css/style.css",
    "dev": "mkdir -p public/css && cp -f node_modules/material-symbols/material-symbols-*.woff2 ./public/css/ && tailwindcss -i ./src/input.css -o ./public/css/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.12.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  },
  "dependencies": {
    "material-symbols": "^0.45.9"
  }
}
